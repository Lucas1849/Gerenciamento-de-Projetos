# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/

# Variáveis de ambiente — NUNCA suba para o GitHub
.env

# Node / Vite
node_modules/
dist/
.cache/

# VS Code
.vscode/

# Sistema operacional
.DS_Store
Thumbs.db

# Banco de dados SQLite (recriado via create_all() — ver ADR-001)
*.db

# Dump de schema do banco do Apoio Hub (referência local, não versionar)
docs/arquitetura/dados/apoio-hub-columns.csv

# Dataset real dos professores orientadores (Fase 21, ADR-023) — dados
# pessoais (nomes, e-mails, respostas de pesquisa) e o repo é público.
# O formato está documentado em professores_seed.exemplo.json; o arquivo
# real é distribuído fora do git (OneDrive da área).
backend/app/dados/professores_seed.json