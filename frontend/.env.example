# URL do backend (lida em src/services/api.js; default sem .env:
# http://127.0.0.1:8000, o uvicorn local do piloto).
# Em produção, apontar para a API hospedada junto ao Hub.
VITE_API_URL=http://127.0.0.1:8000
